{
  "version": 2,
  "builds": [
    {
      "src": "imageuploader/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "imageuploader/wsgi.py" }
  ]
}